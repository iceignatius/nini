#include "ninidump.h"

//------------------------------------------------------------------------------
void ninidump(const nini_root_t *root, char delimiter, const char *filename)
{
    /*
     * Print all data to text file, one item to one line, with format:
     *
     * path/to/the/key <type-in-integer> "<value-in-text-format>" <LF>
     */
}
//------------------------------------------------------------------------------
